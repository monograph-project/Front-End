/* eslint-disable react-refresh/only-export-components -- compatibility barrel for `src/auth/*` */
/**
 * Back-compat entry — SPA auth shell lives under `src/auth/`.
 */
export { AuthProvider, AuthReactContext } from "../auth/AuthProvider";
export { useAuth } from "../auth/useAuth";
